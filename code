from collections import namedtuple
n=int(input()) # total students
data=namedtuple('data',input().split())
m=0 # marks before count
for i in range(n):
    m+= int(data(*input().split()).MARKS)   # sum of all marks
print(m/n) # average
